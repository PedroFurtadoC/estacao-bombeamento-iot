#pragma once

// Copie este arquivo para secrets.h e preencha com os seus dados.
// secrets.h esta no .gitignore e NUNCA deve ser versionado.

#define WIFI_SSID "NOME_DO_HOTSPOT"
#define WIFI_SENHA "SENHA_DO_HOTSPOT"

// IP do notebook que roda o Docker Compose (broker Mosquitto).
// Descubra com "ipconfig": e o IPv4 do adaptador do hotspot.
// No hotspot do Windows normalmente e 192.168.137.1.
#define MQTT_HOST "192.168.137.1"
#define MQTT_PORTA 1883
